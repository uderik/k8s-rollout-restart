package reporter

import (
	"testing"
)

func TestReporter_New(t *testing.T) {
	// Skip this test as it requires a real Kubernetes client
	t.Skip("This test requires a real Kubernetes client")
}

func TestReporter_GenerateReport(t *testing.T) {
	// Skip this test as it requires more complex mocking
	t.Skip("This test requires more complex Kubernetes mocking")
}
